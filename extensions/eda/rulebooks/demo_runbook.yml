---
- name: Run Job Template
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5002

  rules:
    - name: Run Job Template
      condition: event.payload.message == "run-job-template"
      action:
        debug:
          msg: "I'm a debug message."
          
