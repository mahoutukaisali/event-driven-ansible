---
- name: Run Job Template
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        #port: 5003

  rules:
    - name: Run Job Template
      condition: event.payload.message == "Hi"
      action:
        debug:
          msg: "I'm a debug message."
          
